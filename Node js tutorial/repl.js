// R= Read
// E= Evaluate
// P=Print 
// L=Loop repl.js full form 
// Used while using node command line or also progammaticaly

const repl = require("repl");

const local =  repl.start("Welcome to node constole");  // Helps in starting node console node console . Node console can also be started by typing node in the terminal. 